{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "rewrites": [
    { "source": "/en/youtube/(.*)", "destination": "/$1" },
    { "source": "/en/youtube", "destination": "/" }
  ]
}
